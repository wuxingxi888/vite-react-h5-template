---
title: 常见遗漏检查点
impact: HIGH
impactDescription: 分析时必须逐一检查，确保分析完整无遗漏
tags: checklist, common-misses, analysis
---

# 常见遗漏检查点

## 概述

本文档列出设计稿分析时常见的遗漏点，分析时必须逐一检查，确保分析完整。

## 检查清单

分析时必过一遍：

### 四类重中之重（必查）

- [ ] **所有可见文字**：欢迎语、大标题、副标题、说明、按钮/链接/占位/Tab 文案等，按从上到下逐条核对
- [ ] **所有图片/占位图**：尺寸、位置、比例、多图拼接布局，无遗漏
- [ ] **布局**：位置、尺寸、间距、对齐、排列方向、多图拼接方式与设计稿一致，无错位或比例失调
- [ ] **层级**：从外到里的嵌套与先后顺序正确，无丢层或顺序颠倒

详见 `analysis-priorities.md` 获取详细说明。

### 样式相关

- [ ] 背景色（页面、卡片、区块、按钮）
- [ ] 边框（颜色、宽度、圆角）
- [ ] 阴影（颜色、模糊、偏移）
- [ ] 内边距与外边距（padding / gap / margin）
- [ ] 对齐方式（左对齐 / 居中 / 右对齐）

### 区域相关

- [ ] **右侧或对称区域**（易只写左侧漏右侧）
- [ ] **底部或折叠区域**

### 元素相关

- [ ] 小图标、小按钮、Tab 文字
- [ ] 占位图/占位图标尺寸与比例

## 详细说明

### 文字遗漏

**易漏的文字类型**：
- 页面最上方的欢迎语/大标题（最容易在「只记了下面小字」时被漏掉）
- 副标题
- 按钮/链接文案
- 占位提示文字
- Tab 或标签文字

**检查方法**：按「从上到下」扫到时立即记下，不要等到后面再补。

### 图片遗漏

**易漏的图片类型**：
- 小图标
- 占位图
- 多图拼接时的布局关系
- 图片的圆角和比例

**检查方法**：逐区域扫描，确保每张图片都有记录。

### 布局遗漏

**易漏的布局参数**：
- 间距（padding/gap/margin）的方向性
- 对齐方式（左对齐/居中/右对齐）
- 排列方向（横向/纵向/网格）
- 多图拼接时的布局方式

**检查方法**：对照设计稿，确保布局参数与设计稿一致。

### 层级遗漏

**易漏的层级问题**：
- 外层容器被忽略
- 嵌套层级丢失
- 顺序颠倒
- 叠放关系不明确

**检查方法**：按「从外到里」逐层分析，确保每层都有记录。

### 区域遗漏

**易漏的区域**：
- **右侧或对称区域**：只记录了左侧，漏掉右侧
- **底部或折叠区域**：只关注首屏，漏掉底部内容

**检查方法**：按「从上到下、从左到右」完整扫描，确保所有区域都有记录。

## 使用建议

1. **分析时使用**：在第二步「区域与元素提取」时，对照此检查清单逐项检查
2. **完成后验证**：分析完成后，再次对照此检查清单验证是否完整
3. **文档中记录**：在 UI 分析清单的「六、常见遗漏检查点」章节中，列出本次分析已核对的项

## 相关规则

- `analysis-order.md` - 分析顺序（必守）
- `analysis-priorities.md` - 文字、图片、布局、层级四类重中之重
- `workflow-element-extraction.md` - 第二步：区域与元素提取（应用此检查清单）
- `output-analysis-checklist.md` - UI 分析清单文档模板（包含此检查清单章节）
